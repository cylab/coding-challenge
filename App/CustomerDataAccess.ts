import {Customer} from "./Customer";

export class CustomerDataAccess {
    // Never have static methods - they are difficult to mock!
    public addCustomer(customer: Customer) {
        // do not worry

        // fake insert
    }
}
